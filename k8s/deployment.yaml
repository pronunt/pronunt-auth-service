apiVersion: apps/v1
kind: Deployment
metadata:
  name: pronunt-auth-service
  namespace: pronunt-app-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pronunt-auth-service
  template:
    metadata:
      labels:
        app: pronunt-auth-service
    spec:
      containers:
        - name: pronunt-auth-service
          image: ghcr.io/pronunt/pronunt-auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: pronunt-auth-service-config
            - secretRef:
                name: pronunt-auth-service-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20


